
//int main(int argc, char* argv[]) // For completed code
int main()
{
   /*
	if ("1111111111111111" != hexToBinary("FFFF")) { return -1; }
	if ("0000000000000000" != hexToBinary("0000")) { return -1; }

   try {
      char hexChar = 'G';  // Invalid hex character
      std::string binary = hexCharToBinary(hexChar);
      std::cout << "Binary: " << binary << std::endl;
   }
   catch (const std::invalid_argument& e) {
      std::cerr << "Caught exception: " << e.what() << std::endl;
   }
   */

	return 0;
}